import type { NextPage } from "next";
import Head from "next/head";
import Layout from "../components/Layout";

const Blog: NextPage = () => {
  return (
    <Layout>
      <Head>
        <title>Blog | Dilrong</title>
      </Head>
    </Layout>
  );
};

export default Blog;
